# Phase 1 功能分解（Feature Breakdown）

**依据：** PHASE1_SPEC.md（PRD V1.0 §46）
**粒度定义：** 每个 Feature Unit（F x.y）应为可独立开发、独立验收、可演示的最小交付单元，粒度约数天~两周。
**总计：** 10 个顶级功能 → 58 个 Feature Unit，按 4 个里程碑（M1–M4）交付。

---

## 0. 里程碑总览与依赖

```text
M1 底层平台   F1 文档解析（6）+ F10 平台骨架（6）
M2 知识层     F2 企业知识库（6）+ F3 RAG检索（5）+ F4 AI Chat（5）
M3 比对层     F5 规格书对比（6）+ F6 BOM比对（6）
M4 生成层     F7 FMEA生成（6）+ F8 测试用例（6）+ F9 测试报告（6）
```

依赖主线（关键路径）：**F1.6 统一解析模型 → F2 知识库 → F3 RAG → F4 Chat / F7 FMEA**
F5、F6 只依赖 F1，可与 M2 并行开发。
**F10 不可后补**：每个里程碑都必须同步交付对应的横切子项（见 §6 映射表）。

---

## 1. M1 — 底层平台

### F1 文档解析引擎（底层能力）

> 目标：PDF/Word/Excel/图片(含OCR) → 结构化内容，供知识库、比对、生成类功能统一消费。

| ID | Feature Unit | 范围 | 依赖 |
| ---- | ---- | ---- | ---- |
| F1.1 | 文件上传与格式校验 | 类型/大小校验，加密文件拦截，失败时给出明确原因（不静默丢弃） | 无 |
| F1.2 | 版面与表格结构还原 | 版面分析、表格识别、跨页表格合并 | F1.1 |
| F1.3 | 扫描件 OCR | 图片/扫描版 PDF 的文字识别，输出带置信度 | F1.1 |
| F1.4 | 工程参数抽取 | 容量/温度/扭矩/公差等 值+单位 的键值对抽取 | F1.2 |
| F1.5 | 解析结果人工校对视图 | 低置信度区域标红，人工修正回写 | F1.2–F1.4 |
| F1.6 | 统一解析输出模型 | 文档 → 章节 → 表格/段落 → 字段键值对，作为全平台唯一解析数据契约 | F1.1 |

**验收（F1 整体）：** 真实电池规格书（含参数表）解析字段准确率 ≥ 90%；解析失败原因明确。

### F10 工程确认与追溯机制（横切，M1 交付骨架）

| ID | Feature Unit | 范围 | 依赖 |
| ---- | ---- | ---- | ---- |
| F10.1 | 统一对象模型（Phase 1 子集） | Project / Product / Document / BOM / FMEA / TestCase / TestReport / Issue / Risk 及关联关系；为二阶段扩展预留 | 无（最优先） |
| F10.2 | 人工确认状态机 | 所有 AI 生成物默认"草稿"态 → 人工采纳/修改 → 定版；不提供 AI 直写正式系统通路 | F10.1 |
| F10.3 | AI 审计日志 | 每次AI操作记录：用户/时间/项目/输入/模型/Prompt版本/知识库版本/引用数据/AI输出/人工修改/最终结果 | F10.1 |
| F10.4 | 审计查询页（最小版） | 按 用户/项目/时间 检索 AI 操作历史（UI 页面28 最小版） | F10.3 |
| F10.5 | RBAC 基础版 | 用户/角色/部门/项目四级权限；预置角色：工程师/项目经理/研发主管/系统管理员/AI管理员（PRD §5） | F10.1 |
| F10.6 | KPI 埋点 | 各功能流程耗时打点、AI建议采纳/修改事件埋点，支撑 §5 全部 KPI 报表 | F10.1 |

**M1 完成定义（Exit）：** 真实规格书样本集解析准确率 ≥ 90% 且失败原因可读；对象模型/状态机/审计/RBAC 骨架可运行并有集成测试。

---

## 2. M2 — 知识层

### F2 企业知识库（依赖 F1.6）

| ID | Feature Unit | 范围 | 依赖 |
| ---- | ---- | ---- | ---- |
| F2.1 | 文档上传与版本管理 | 单个/批量上传、去重、同名文档多版本 | F1.6 |
| F2.2 | 分类体系 | 产品/设计/BOM/FMEA/测试/质量/SOP 七类分类树（对齐 UI 页面22） | F2.1 |
| F2.3 | 文档关联 | 文档 ↔ 项目、文档 ↔ 产品 关联 | F2.1, F10.1 |
| F2.4 | 全文搜索与筛选 | 全文检索 + 分类/项目/时间/标签筛选 | F2.1 |
| F2.5 | 最近更新与 AI 推荐 | 最近更新列表；"与当前项目相关的历史案例 N 个"推荐 | F2.3, F3.1 |
| F2.6 | 文档权限继承 | 按项目/部门控制可见性；私有化部署（数据不出企业域） | F10.5 |

**验收（F2 整体）：** 每份文档可追溯 来源/上传人/版本历史。

### F3 AI 知识检索 RAG（依赖 F1, F2）

| ID | Feature Unit | 范围 | 依赖 |
| ---- | ---- | ---- | ---- |
| F3.1 | 混合检索 | 自然语言提问 → 向量 + 关键词混合检索 | F2.1 |
| F3.2 | 带引用答案输出 | 答案必含：来源文档、项目、时间、原文片段（PRD §36） | F3.1 |
| F3.3 | 多轮追问 | 追问上下文保持 | F3.2 |
| F3.4 | 检索结果操作 | 查看原文定位；关联到当前 FMEA/项目 | F3.2 |
| F3.5 | 无命中兜底 | 无命中时明确回答"未找到"，禁止无依据作答 | F3.2 |

**验收（F3 整体）：** 典型问法（密封失效案例 / 历史DFM问题 / 料号使用项目）可答且出处正确，可抽样审计。

### F4 AI Chat / AI 助手（依赖 F1–F3）

| ID | Feature Unit | 范围 | 依赖 |
| ---- | ---- | ---- | ---- |
| F4.1 | 对话框与文件上传 | 自然语言 + 文件上传；CAD 文件仅提示"暂不支持解析" | 无 |
| F4.2 | 意图识别与路由 | 路由到 6 个 Phase 1 技能：规格书对比/BOM比对/FMEA生成/测试用例/测试报告/知识检索 | F3, F5–F9 入口 |
| F4.3 | 项目上下文绑定 | 对话绑定项目，后续任务继承上下文 | F10.1 |
| F4.4 | AI 任务卡 | 任务名/状态/风险统计/后续动作按钮（查看报告/生成FMEA/生成整改任务）（PRD §7.3） | F4.2 |
| F4.5 | 异步任务与通知 | 生成类任务走异步队列 + 进度通知（PRD §52） | F4.2 |

**验收（F4 整体）：** 简单问答首次响应 ≤ 5s；生成类任务全部异步。

**M2 完成定义（Exit）：** 文档来源/上传人/版本可追溯；RAG 答案带正确出处、无命中时明示；Chat 首响 ≤ 5s；文档权限按项目生效并写入审计。

---

## 3. M3 — 比对层

### F5 规格书智能对比（依赖 F1）

| ID | Feature Unit | 范围 | 依赖 |
| ---- | ---- | ---- | ---- |
| F5.1 | 同模板字段级 Diff | 参数 / A值 / B值 / 差异 三列对比 | F1.4 |
| F5.2 | 跨模板 AI 语义映射 | Nominal Capacity ≈ Rated Capacity ≈ Typical Capacity 类映射（PRD §21） | F5.1 |
| F5.3 | 差异重要性分级 | 差异类型 + 🔴🟠🟡 分级 | F5.1 |
| F5.4 | AI 差异总结 | 自然语言结论（如"循环寿命存在明显差异"） | F5.3 |
| F5.5 | 对比结果导出 | Excel / PDF | F5.3 |
| F5.6 | 映射关系沉淀 | 人工确认后的映射关系入库复用 | F5.2, F10.2 |

**验收（F5 整体）：** 文档比对时间 ↓ ≥ 80%（PRD §50，需 M1 埋点提供基线对照）。

### F6 BOM 智能比对（依赖 F1）

| ID | Feature Unit | 范围 | 依赖 |
| ---- | ---- | ---- | ---- |
| F6.1 | BOM 导入与列映射 | Excel 为主、支持 PDF 表格；列映射：料号/名称/数量/层级/版本/替代料 | F1.2 |
| F6.2 | 对齐与核对引擎 | 按料号+层级对齐；检查 数量/版本/新增/缺失/替代料/状态（PRD §20） | F6.1 |
| F6.3 | 风险分级与 AI 分析 | 差异 高/中/低 分级 + AI 归因（如"PLM未更新""请确认"） | F6.2 |
| F6.4 | 比对总览看板 | 总物料数/一致/差异/风险分布（UI 页面12） | F6.3 |
| F6.5 | 差异逐条处置 | 确认 / 忽略 / 转整改任务（走 F10.2 状态机，处置入审计） | F6.3, F10.2 |
| F6.6 | 比对报告导出 | PDF / Excel / Word | F6.4 |

**不做：** PLM/ERP 系统直连提交（第二阶段）。
**验收（F6 整体）：** BOM 核对时间 ↓ ≥ 70%（PRD §50）。

**M3 完成定义（Exit）：** 两类比对均可端到端跑通（导入→比对→人工处置→导出）；比对结论默认草稿态、定版需人工确认；耗时 KPI 可用埋点数据验证。

---

## 4. M4 — 生成层

### F7 AI FMEA 生成（依赖 F2, F3）

| ID | Feature Unit | 范围 | 依赖 |
| ---- | ---- | ---- | ---- |
| F7.1 | 五维链生成 | 功能 → 失效模式 → 失效影响 → 失效原因 → 控制措施（PRD §26） | F1.6, F2 |
| F7.2 | S/O/D 与 RPN | S/O/D 打分建议 + RPN 计算 + 风险色标（UI 页面16） | F7.1 |
| F7.3 | 历史依据引用 | 每条建议附知识库案例引用（"查看历史案例"） | F3.4 |
| F7.4 | 专业表格编辑 | 行内编辑、增删行、批量采纳/忽略 AI 建议 | F7.1 |
| F7.5 | 人工审核流 | AI初稿 → 工程师修改确认 → 定版（PRD §43） | F7.4, F10.2 |
| F7.6 | FMEA 导出 | Excel / Word | F7.4 |

**验收（F7 整体）：** FMEA 初稿时间 ↓ ≥ 60%；AI 建议采纳率/修改率可统计。

### F8 测试需求与用例生成（依赖 F1, F2）

| ID | Feature Unit | 范围 | 依赖 |
| ---- | ---- | ---- | ---- |
| F8.1 | 输入资料勾选 | 规格书/客户需求/企业测试标准/历史用例（UI 页面20） | F2 |
| F8.2 | 测试需求输出 | 测试项目/目的/条件/方法/设备/样本/判定标准（PRD §30） | F8.1 |
| F8.3 | 测试用例输出 | Case ID/前置条件/输入条件/操作步骤/预期结果/判定标准/测试设备（PRD §31） | F8.1 |
| F8.4 | 生成统计 | 新增 N 条 / 复用 N 条（复用自历史用例库） | F8.2, F8.3 |
| F8.5 | 逐条处置 | 采用 / 编辑 / 查看依据（来源标准或历史用例引用） | F8.2, F8.3 |
| F8.6 | 用例库管理 | 项目内用例集；状态跟踪：未开始/进行中/通过/失败 | F8.3, F10.2 |

**验收（F8 整体）：** 测试需求覆盖率可统计。

### F9 测试报告生成（依赖 F8）

| ID | Feature Unit | 范围 | 依赖 |
| ---- | ---- | ---- | ---- |
| F9.1 | 测试数据导入 | Excel / CSV | F10.1 |
| F9.2 | 自动统计与图表 | 样本数 / Pass / Fail、图表生成 | F9.1 |
| F9.3 | 异常项分析 | 定位异常样本、超限幅度、AI 整改建议（UI 页面21） | F9.2 |
| F9.4 | 报告正文生成 | 概要/数据统计/图表/Pass-Fail/异常项/结论/附件（PRD §32） | F9.2, F9.3 |
| F9.5 | 报告导出 | PDF / Word | F9.4 |
| F9.6 | 后续联动 | 生成 FMEA 风险条目；创建问题单（写入知识库质量问题分类） | F9.3, F7, F10.1 |

**验收（F9 整体）：** 测试报告制作时间 ↓ ≥ 70%；测试结论进入正式体系前必须人工确认（PRD §43）。

**M4 完成定义（Exit）：** 三类生成物完整走 草稿→人工确认→定版 闭环；采纳率/修改率报表可用；§5 全部 KPI 有埋点数据支撑。

---

## 5. 建议开发顺序（里程碑内）

```text
M1:  F10.1 对象模型 → F10.5 RBAC → F10.2 状态机 → F10.3 审计 →（F10.4/F10.6 页面与报表可后置到M2/M3）
     F1.1 上传 → F1.6 解析模型 → {F1.2 版面 ∥ F1.3 OCR ∥ F1.4 参数抽取} → F1.5 校对视图

M2:  F2.1–F2.3 → F2.4 → {F2.5 ∥ F2.6}
     F3.1 → F3.2 → {F3.3 ∥ F3.4 ∥ F3.5}
     F4.1/F4.3 → F4.2 → {F4.4 ∥ F4.5}

M3:  F5.1 → F5.2 → {F5.3 ∥ F5.4} → {F5.5 ∥ F5.6}
     F6.1 → F6.2 → {F6.3 ∥ F6.4} → {F6.5 ∥ F6.6}
     （M3 与 M2 可部分并行：F5/F6 只依赖 F1）

M4:  F7.1 → {F7.2 ∥ F7.3} → F7.4 → F7.5 → F7.6
     F8.1 → {F8.2 ∥ F8.3} → {F8.4 ∥ F8.5} → F8.6
     F9.1 → F9.2 → F9.3 → F9.4 → {F9.5 ∥ F9.6}
```

---

## 6. F10 横切子项 × 里程碑落地映射

| F10 子项 | M1 | M2 | M3 | M4 |
| ---- | ---- | ---- | ---- | ---- |
| F10.1 对象模型 | ✅ 全量定义 | 扩展知识库关联 | 扩展比对对象 | 补充 TestCase/TestReport/Issue/Risk 联动 |
| F10.2 人工确认状态机 | ✅ 骨架 | 文档定版接入 | 比对结论接入 | FMEA/用例/报告接入（完整闭环） |
| F10.3 AI 审计日志 | ✅ 骨架 | 记录RAG引用与知识库版本 | 记录差异处置动作 | 记录采纳/修改（供KPI） |
| F10.4 审计查询页 | 骨架（API） | ✅ 最小版上线 | 增加比对筛选 | 增加生成类筛选 |
| F10.5 RBAC | ✅ 四级权限+预置角色 | 文档权限继承生效 | 比对对象权限 | 生成物编辑/定版权限 |
| F10.6 KPI 埋点 | ✅ 打点框架 + 人工基线测量 | 首响≤5s 监控 | 比对/BOM 耗时报表 | 采纳率/修改率报表 |

> 注意：§5 的 KPI 是"时间下降百分比"，必须有 **人工基线测量**（Phase 1 开始时记录人工流程耗时），否则 M3/M4 无法验收。

---

## 7. 明确不在 Phase 1 范围（防止蔓延）

| 内容 | 归属 |
| ---- | ---- |
| DFM规则/装配干涉检查/仿真 | 第二阶段 |
| 制程参数/电子工作牌/SOP与QCP校验 | 第二阶段 |
| PLM/ERP/MES/QMS 深度集成（BOM 仅文件导入） | 第二阶段 |
| CAD特征识别/3D→2D/自然语言设计/CAE | 第三阶段 |
| 跨系统自主工程 Agent | 第四阶段 |
| 项目风险雷达/工程变更影响分析 | 暂缓 |
