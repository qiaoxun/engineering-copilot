# Phase 1 功能规格（第一阶段）

**依据：** PRD V1.0 §46（MVP版本建议 - 第一阶段）、UI_GUIDE 相关页面
**范围声明：** AI知识库 + AI助手 + BOM/文档智能化 + FMEA + 测试
**选择原因：** 数据基础容易建立，业务价值容易验证（PRD §46）

---

## 0. Phase 1 目标

让工程师在不接入 PLM/ERP/MES/CAD 的前提下，仅通过文档与表格即可完成：

> 知识沉淀 → 智能问答 → 文档/BOM比对 → FMEA初稿 → 测试用例与报告生成

并建立 Phase 1 必须落地的三项平台机制：**人工确认流程、AI可追溯、RBAC基础权限**。

---

## 1. 功能清单总览

| 编号 | 功能 | 来源 | 对应UI页面 | 依赖 |
| ---- | ---- | ---- | ---- | ---- |
| F1 | 文档解析引擎（PDF/Word/Excel） | PRD §46.3 | — （底层能力） | 无 |
| F2 | 企业知识库 | PRD §46.1、§35 | 页面22 企业知识库 | F1 |
| F3 | AI知识检索（RAG问答） | PRD §36 | 页面23 AI知识检索 | F1, F2 |
| F4 | AI Chat / AI助手 | PRD §7 | 页面01 首页 + 右侧Copilot | F1–F3 |
| F5 | 规格书智能对比 | PRD §46.4、§21 | 页面13 规格书对比 | F1 |
| F6 | BOM智能比对 | PRD §46.5、§19–20 | 页面11/12 BOM工作台与比对 | F1 |
| F7 | AI FMEA生成 | PRD §46.6、§26 | 页面16 FMEA工作台 | F2, F3 |
| F8 | 测试需求与用例生成 | PRD §46.7、§30–31 | 页面19/20 测试工作台 | F1, F2 |
| F9 | 测试报告生成 | PRD §46.8、§32 | 页面21 测试报告 | F8 |
| F10 | 工程确认与追溯机制（横切） | PRD §43–44、§42 | 贯穿全部页面 | 无 |

---

## 2. 功能明细

### F1 文档解析引擎（底层能力）

**目标：** 将 PDF / Word / Excel / 图片(含扫描件OCR) 解析为结构化内容，供知识库、比对、生成类功能统一消费。

功能点：
1. PDF/Word/Excel 上传与格式校验（大小、类型、加密文件拦截）
2. 版面与表格结构还原（跨页表格合并）
3. 扫描件 OCR
4. 工程参数抽取（容量/温度/扭矩/公差等 值+单位）
5. 解析结果人工校对视图（解析置信度低的区域标红）
6. 统一解析输出模型：文档 → 章节 → 表格/段落 → 字段键值对

验收要点：
- 常见电池规格书（含参数表）解析字段准确率 ≥ 90%
- 解析失败时给出明确原因，不静默丢弃

---

### F2 企业知识库

**目标：** 建立企业级工程文档统一存储与分类管理（PRD §35 的 Phase 1 子集）。

功能点：
1. 文档上传/批量上传、去重、版本管理（同名文档多版本）
2. 分类体系：产品 / 设计 / BOM / FMEA / 测试 / 质量 / SOP（对齐UI页面22左侧分类树）
3. 文档与项目、产品关联
4. 全文搜索 + 筛选（分类/项目/时间/标签）
5. 最近更新、AI推荐知识（"与当前项目相关的历史案例 N 个"）
6. 文档权限继承（按项目/部门控制可见性）

验收要点：
- 每份文档可追溯到来源、上传人、版本历史
- 支持私有化部署（数据不出企业域，PRD §53）

---

### F3 AI知识检索（RAG问答）

**目标：** 面向知识库的自然语言问答，答案带引用。

功能点：
1. 自然语言提问 → 向量 + 关键词混合检索
2. 答案输出必须携带：来源文档、项目、时间、原文片段（PRD §36）
3. 追问/多轮对话
4. 检索结果操作：查看原文定位、关联到当前FMEA/项目
5. 无命中时明确回答"未找到"，禁止无依据作答（抑制幻觉）

示例场景（PRD §36）：
- "过去3年类似电池包的密封失效案例"
- "这个结构有没有历史DFM问题？"
- "哪些项目使用过这个料号？"

---

### F4 AI Chat / AI助手

**目标：** 工作台统一入口，"告诉AI你要做什么"（PRD §7）。

功能点：
1. 对话框：自然语言 + 文件上传（CAD文件类型Phase 1仅做提示，不解析）
2. 意图识别并路由到 Phase 1 技能：规格书对比 / BOM比对 / FMEA生成 / 测试用例 / 测试报告 / 知识检索
3. 项目上下文选择（对话绑定项目）
4. **AI任务卡**（PRD §7.3）：任务名、状态、风险统计、后续动作按钮（查看报告 / 生成FMEA / 生成整改任务）
5. 复杂任务异步执行 + 进度通知（PRD §52 性能要求）

验收要点：
- 简单问答首次响应 ≤ 5s；生成类任务走异步队列

---

### F5 规格书智能对比

**目标：** 两份规格书（PDF/Word）自动字段级对比。

功能点：
1. 同模板：字段级 Diff（参数 / A值 / B值 / 差异）
2. 跨模板：AI字段语义映射（如 Nominal Capacity ≈ Rated Capacity ≈ Typical Capacity，PRD §21）
3. 差异输出含：差异类型、重要性分级（🔴🟠🟡）
4. AI差异总结（自然语言结论，如"循环寿命存在明显差异"）
5. 对比结果导出（Excel/PDF）
6. 映射关系人工确认后沉淀，供后续复用

验收要点：文档比对时间下降 ≥ 80%（PRD §50 KPI）

---

### F6 BOM智能比对

**目标：** 设计BOM 与 PLM/ERP BOM（Phase 1 以文件形式导入）自动核对。

功能点：
1. BOM导入（Excel为主，支持PDF表格）与列映射（料号/名称/数量/层级/版本/替代料）
2. 按料号+层级对齐，检查：数量 / 版本 / 新增 / 缺失 / 替代料 / 状态（PRD §20）
3. 差异风险分级（高/中/低）+ AI分析（如"PLM未更新""请确认"）
4. 比对总览：总物料数 / 一致 / 差异 / 风险分布（UI页面12）
5. 差异逐条人工处置：确认 / 忽略 / 转整改任务
6. 比对报告导出（PDF/Excel/Word）

不做（Phase 1）：与 PLM/ERP 系统直连提交（PRD 将深度集成放在第二阶段）。

验收要点：BOM核对时间下降 ≥ 70%（PRD §50 KPI）

---

### F7 AI FMEA生成

**目标：** 基于规格书/结构信息 + 知识库历史案例，生成 FMEA 初稿。

功能点：
1. 五维链生成：功能 → 失效模式 → 失效影响 → 失效原因 → 控制措施（PRD §26）
2. S/O/D 打分建议 + RPN 计算 + 风险色标（UI页面16）
3. 每条建议附历史依据（来自知识库的案例引用，"查看历史案例"）
4. 专业表格编辑：行内编辑、增删行、批量采纳/忽略AI建议
5. 人工审核流：AI初稿 → 工程师修改确认 → 定版（PRD §43）
6. FMEA 导出（Excel/Word）

验收要点：FMEA初稿时间下降 ≥ 60%（PRD §50 KPI）；AI建议采纳率/修改率可统计（PRD §50 AI类KPI）

---

### F8 测试需求与用例生成

**目标：** 从规格书、客户需求、企业标准、历史用例生成测试需求与用例。

功能点：
1. 输入资料勾选（规格书/客户需求/企业测试标准/历史用例，UI页面20）
2. 测试需求输出（PRD §30）：测试项目/目的/条件/方法/设备/样本/判定标准
3. 测试用例输出（PRD §31）：Case ID/前置条件/输入条件/操作步骤/预期结果/判定标准/测试设备
4. 生成统计：新增 N 条 / 复用 N 条（复用自历史用例库）
5. 逐条处置：采用 / 编辑 / 查看依据（依据 = 来源标准或历史用例引用）
6. 用例库管理（项目内用例集、状态跟踪：未开始/进行中/通过/失败）

验收要点：测试需求覆盖率可统计（PRD §50 质量类KPI）

---

### F9 测试报告生成

**目标：** 从测试数据 + 用例 + 判定标准自动生成测试报告。

功能点：
1. 测试数据导入（Excel/CSV）
2. 自动统计：样本数 / Pass / Fail、图表生成
3. 异常项分析：定位异常样本、超限幅度、AI整改建议（UI页面21）
4. 报告正文生成：概要/数据统计/图表/Pass-Fail/异常项/结论/附件（PRD §32）
5. 报告导出（PDF/Word）
6. 后续联动：生成FMEA风险条目、创建问题单（写入知识库质量问题分类）

验收要点：测试报告制作时间下降 ≥ 70%（PRD §50 KPI）；测试结论进入正式体系前必须人工确认（PRD §43）

---

### F10 工程确认与追溯机制（横切能力）

**目标：** 满足 PRD §43/§44/§42 的强制性平台要求，Phase 1 必须落地最小闭环。

功能点：
1. **人工确认流程**：所有AI生成物（对比结论、FMEA、用例、报告）默认"草稿"态，经人工采纳/修改后才算定版；不提供AI直写正式系统的通路
2. **AI审计日志**：每次AI操作记录 用户/时间/项目/输入/模型/Prompt版本/知识库版本/引用数据/AI输出/人工修改/最终结果（PRD §44）
3. **审计查询页**：按用户/项目/时间检索AI操作历史（UI页面28的最小版）
4. **RBAC基础版**：用户/角色/部门/项目四级权限 + 文档权限继承；预置角色对齐 PRD §5（工程师/项目经理/研发主管/系统管理员/AI管理员）
5. **统一对象模型（Phase 1 子集）**：Project / Product / Document / BOM / FMEA / TestCase / TestReport / Issue / Risk 及其关联关系（PRD §40 子集），为二阶段扩展预留

---

## 3. 建议交付顺序

```text
F1 文档解析 ──→ F2 知识库 ──→ F3 RAG检索 ──→ F4 AI Chat
      │                                │
      ├──→ F5 规格书对比               ├──→ F7 FMEA生成（依赖历史案例引用）
      └──→ F6 BOM比对                 └──→ F8 测试用例 ──→ F9 测试报告

F10 确认/追溯/权限：随各功能同步落地，不可后补
```

里程碑建议：
- **M1（底层）：** F1 + F10(权限/审计骨架) + 统一对象模型
- **M2（知识）：** F2 + F3 + F4
- **M3（比对）：** F5 + F6
- **M4（生成）：** F7 + F8 + F9

---

## 4. 明确不在 Phase 1 范围

| 内容 | 归属 |
| ---- | ---- |
| DFM规则/装配干涉检查/仿真 | 第二阶段 |
| 制程参数/电子工作牌/SOP与QCP校验 | 第二阶段 |
| PLM/ERP/MES/QMS 深度集成（BOM比对仅文件导入） | 第二阶段 |
| CAD特征识别/3D→2D/自然语言设计/CAE | 第三阶段 |
| 跨系统自主工程Agent | 第四阶段 |
| 项目风险雷达/工程变更影响分析 | 依赖系统集成，暂缓 |

---

## 5. Phase 1 验收 KPI（对齐 PRD §50）

| 指标 | 目标 |
| ---- | ---- |
| 文档比对时间 | ↓ 80%+ |
| BOM核对时间 | ↓ 70%+ |
| FMEA初稿时间 | ↓ 60%+ |
| 测试报告制作时间 | ↓ 70%+ |
| AI建议采纳率 / 人工修改率 | 可统计、可报表 |
| AI引用准确率（RAG答案带正确出处） | 可抽样审计 |
